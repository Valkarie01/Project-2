{% layout 'layout.liquid' %}

{% block content %}
<div>
    <h2 class="display-6">Welcome! Great to see you!</h2>
    <div class="row">
            <div class="card" style="width: 20rem">
                <h5 class="card-header">{{artist.name}}</h5>
            
                    <p class="card-text">
                        {{artist.name}}
                    </p>
                    <a href="/artist/{{artist._id}}" class="btn btn-info">View {{artist.name}}</a>
                </div>
            </div>
    </div>
</div>
{% endblock %}